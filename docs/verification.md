# Verification

## September 26, 2026 operational hardening

The new `check:native-consumer` command packed the current 0.7.1 source into fresh disposable Capacitor 8 consumers, applied the pinned native overlay, synced public trust, and compiled an iOS Simulator app with Xcode and an Android debug app with Gradle/JDK 21. `doctor` passed again after each build. The macOS check and tag-release workflows now require this build. Those hosted jobs have not run: the GitHub account billing lock described below still prevents jobs from starting. No package or Marketplace release was published.

After these changes, `npm run check` passed locally: 79 tests passed, zero failed, and three environment-gated tests were skipped. The command also passed TypeScript checking, the Cloudflare Worker type check and dry run, and the library build. `npm audit --omit=dev --audit-level=high` reported zero vulnerabilities; script syntax, workflow YAML parsing, and `git diff --check` passed.

In a dedicated temporary Cloudflare Worker/D1/R2 service, live read-only and write-mode `test-provider` passed. The write test rejected unauthorized, tampered, and replayed commands; checked create-only upload, signed metadata, complete and ranged artifacts, competing atomic promotions, rollback, and withdrawal. A synthetic Capacitor 8 app on an iOS Simulator and a separate Android emulator each installed and launched bundle A, activated signed bundle B, briefly activated an intentionally unready C, and returned to B after the native startup watchdog. Signed operator rollback then advanced the channel heads to iOS sequence 4 and Android sequence 3. Remote doctor verified signed metadata, artifact size, ranges, and SHA-256. These are simulator/emulator results, not physical-device acceptance of this hosted provider or a production app.

A hosted restore rehearsal exported the stopped-for-publishing D1 state, imported it into a second dedicated D1 database, copied all six immutable R2 objects to a second private bucket, and verified every copied object's size and SHA-256. The restored channel heads matched the original. Rebinding the test Worker to the restored D1/R2 passed Android `doctor --remote` at sequence 3, including full artifact integrity. A Node provider stopped-state copy also passed its automated restore test. This demonstrates one consistent synthetic restore, not a measured recovery time objective or a guarantee that an older backup can be served safely without reconciling later channel sequences.

The bounded, read-only metadata benchmark against the restored hosted service completed 100 Android requests at concurrency 10: 100 successful signed manifests, zero errors, 14.06 client-observed requests/second, p50 738.58 ms, p95 779.68 ms, and p99 785.55 ms for the complete response. This is a single-client, single-region sample with tiny artifacts; it does not measure CDN cache hits, realistic 3 MB downloads, retry bursts, provider-side p99, or 100,000 devices. A representative fleet test would exceed the user's zero-cost test constraint. Use the runbook's staged traffic plan and record cost, cache, and regional metrics before making a fleet-capacity claim.

The temporary Worker, both D1 databases, both R2 buckets, their objects, the dedicated iOS Simulator, and the dedicated Android AVD were removed. Wrangler then listed no D1 databases or R2 buckets in this test account, and the former Worker URL returned 404. Published free-tier allowances covered this small test's intended usage, but account billing data was unavailable to this run; an account owner must confirm the final invoice. Hosted Supabase and its security-advisor results remain unverified because the isolated live acceptance used Cloudflare instead.

## 0.7.1 patch candidate and acceptance limits

On September 26, 2026, the Node provider's upload capability was moved from a URL query to the `X-Direct-OTA-Upload` header. The real HTTP tests exercised valid upload, repeat upload, missing header, a query-only capability, a query plus valid header, and interrupted/corrupt uploads. The CLI already accepts this header; the changed provider does not require a new native binary.

With JDK 21 on the path, `npm run check` passed: 78 tests passed, none failed, and three environment-gated tests were skipped in that command. It also ran TypeScript checking, the Cloudflare Worker type check and dry run, and the library build. `npm run test:supabase-local` separately passed both Docker Storage and PostgreSQL checks. The Firebase Firestore/Storage emulator conformance test passed separately, as did the Firebase Function type check and the root and Firebase dependency audits (zero reported vulnerabilities). The repository has no separate lint script; `git diff --check` passed.

A local comparative metadata sample ran 400 requests per round against the previous and changed Node provider, three rounds each, with 16 concurrent clients. All 2,400 requests succeeded. In the two warmed rounds, previous-provider p95 was 2.78 and 2.69 ms; changed-provider p95 was 2.50 and 2.60 ms. The sample shows no observed local metadata regression in this small process-level test. It is not a load or latency guarantee for a hosted service or large fleet.

An isolated Capacitor 8 fixture was signed with an existing development profile, installed, and launched on a paired physical iPhone. Its public trust pointed to a temporary HTTPS tunnel backed by the local Node provider and a synthetic publisher identity. The normal CLI prepared, uploaded, and promoted an iOS internal release (sequence 1, release `0c2ee5a1-4838-4532-8418-36c8664db95f`); remote doctor verified the signature, artifact size, byte ranges, and complete SHA-256. The service then observed the iPhone's `POST /check` (200), native artifact `GET` (200), and a request from the updated B bundle after `markReady` completed. The app process remained running. The disposable app was uninstalled, and the local service and tunnel were stopped. This proves one real physical-device download, activation, and local readiness of the synthetic fixture. It does not establish a production app's business flows, broad device compatibility, or fleet health.

The disposable local Supabase command passed against actual Docker Storage and PostgreSQL: Storage authorization, immutable upload and ranged public delivery, plus SQL privileges, replay, compare-and-swap, rollback, withdrawal, and restrictive policies. The Firebase Firestore/Storage emulator conformance test also passed. A read-only account inventory found five Supabase projects and no clearly dedicated OTA test project; no hosted Supabase project was created or changed. Hosted acceptance still needs an isolated OTA-only project with confirmed no-charge capacity, reviewed target and migration, the write-mode provider conformance test, remote doctor, and security-advisor results. A local emulator cannot substitute for that evidence.

No representative 100,000-device load run has been performed. The next capacity test needs a production-like isolated provider/CDN, an agreed traffic and artifact-size profile, approved bandwidth budget, and measured request/error rates, p50/p95/p99 metadata latency, egress, cache hit rate, and retry amplification. Small local or single-tunnel samples are regression signals, not a fleet capacity claim.

The public npm registry returned `E404` for `direct-ota` at this check; no npm publication was attempted. A clean-commit `release:verify` tarball and checksum can prove package bytes and consumer installation, but registry authentication and publication remain separate maintainer actions. The `v0.7.0` GitHub Actions check and package-release runs never started jobs: GitHub reported, “The job was not started because your account is locked due to a billing issue.” No billing settings were changed. CI acceptance requires the account owner to resolve the lock and a new run to complete; local checks do not count as a passing hosted workflow.

A local scan of 366 historical Git blobs found no high-confidence private-key block, GitHub token, Supabase secret token, AWS access key, or secret-named tracked path. This is a bounded heuristic scan, not proof that every credential format is absent.

## 0.7.0 local release checks

On September 26, 2026, `npm run check` passed with JDK 21: 78 tests passed, none failed, and three environment-gated tests were skipped in that run. It included TypeScript checking, the Node HTTPS publishing integration, the Cloudflare local Worker integration and dry run, deterministic malformed-manifest checks, key staging, and the new compound delta release test. A separate local Supabase command (`npm run test:supabase-local`) passed with actual Docker Storage plus an isolated PostgreSQL migration check. Synthetic anonymous/customer tokens could not upload or delete; a scoped capability uploaded once, overwrite failed, public bytes and `206` ranges matched, and the SQL checks covered privileges, replay, compare-and-swap, rollback, withdrawal, and restrictive policies. No hosted Supabase project was created or modified.

The delta integration prepared a mostly unchanged web bundle against a prior promoted candidate through the public CLI, uploaded and promoted the compound object through the Node provider, fetched both signed `206` ranges, decrypted the full and patch segments, and reconstructed the same ZIP from the verified base. A changed random asset correctly produced a normal full release when a delta would not save bytes; altered and symlinked local base candidates were rejected. Native Swift and Android Java tests reconstructed a bounded patch and rejected a corrupted patch; the native iOS Simulator Xcode build and Android Gradle debug build passed after applying the overlay to a disposable Capacitor 8 consumer. `doctor` confirmed both synced native configurations and the runtime. The fixture was signed, installed, and launched on iphone80888; the Android APK was installed and launched on an Android 36 emulator. This synthetic fixture did not perform a network OTA or validate production app behavior, so these checks do not prove physical-device OTA activation.

Key rotation was locally exercised by staging a second private publisher identity, pinning the public key ring into the disposable native fixture, activating the next key after native synchronization, and verifying signature admission and monotonic key acceptance. Rotation requires a store build carrying the new key ring before publisher activation. Neither this local fixture nor the Node HTTPS integration establishes adoption on production devices.

The published tags through `v0.6.0` retain earlier native overlay hashes recorded in `native/overlay-history.json`. Version 0.5.0 has no tag in this checkout, so its independently distributed package needs inspection before claiming a verified upgrade path. The patcher accepts known prior overlays and rejects unknown modifications. This 0.7.0 source adds the exact previous-commit overlay hashes so upgrades from the locally tested pre-release overlay can converge too.

## 0.6.0 setup and doctor changes

Version 0.6.0 adds a per-gate doctor report, restricted local repairs, and a reviewed finish path for dedicated Cloudflare/Firebase services. The local suite exercises read-only planning, JSON and TypeScript config merging, preservation of other plugin settings, native sync convergence with a synthetic runner, deployment ordering and target scoping with injected runners, malformed generated settings, runtime drift, and refusal to overwrite a different updater identity. `npm run check` passed with 56 tests passing and three environment-dependent tests skipped; it includes type checking, the Cloudflare Worker dry run, and the library build. Separately, a package tarball of these changes was installed in a disposable Capacitor 8 app, `cap add ios` created a real iOS project, guided setup ran, and `setup --finish` completed an actual `cap sync` plus a passing local `doctor`. Upgrading the fixture to the 0.6.0 tarball changed its native dependency lock, and doctor correctly rejected the old runtime until `native`, JSON repair, and `cap sync` produced a matching configuration. The provider deployment, conformance response, and remote metadata were injected for this fixture, so no account was changed. No physical app was installed or hosted capacity measured for these changes. `setup --finish` still requires pre-provisioned dedicated provider resources; it does not handle shared Supabase migrations, complex/dynamic Capacitor configs, app coordinator wiring, or device acceptance.

The following checks were performed on the generalized Direct OTA package. The [case study](case-study.md) separately describes observations from the original integration.

## 0.5.0 automated baseline

- TypeScript type checking and production library build.
- Signed manifest and command validation, including wrong keys/purposes, altered signatures, exact artifact hosts/paths, bounds, and compatibility.
- Public-configuration rejection of private PEM/JWK material, private identity permissions, and create-only initialization.
- Packaging rejection of credential markers, source maps, links, unsafe entries, and oversized content.
- Native patch hashes, idempotent installation, drift rejection, trust/source fingerprints, and stale generated/native configuration detection.
- Synthetic native Swift signature verification, shared Swift/Java/TypeScript SemVer corpus, and CLI-generated bundle decryption through the pinned upstream Swift CryptoCipher.
- Client activity guards, retained mandatory requirements, timed retries, background/offline pauses, permanent-error handling, and metadata retry throttling.
- Standalone provider authorization, replay, concurrent publication, immutable artifacts, byte ranges, and persistence.
- Supabase handler admission, bounded runtime catalog lookup, signature checks, and verified-artifact promotion.
- SQL migration, privileges, restrictive Storage policies against deliberately broad legacy policies, replay, concurrent compare-and-swap, rollback, and direct withdrawal in an isolated PostgreSQL 17 database.
- A local HTTPS integration using the real CLI: initialize, prepare, upload twice, promote, download/hash-check, staged rollout, rollback, and withdraw.
- The short `publish` path: host build failure leaves the channel untouched; a successful build creates, uploads, promotes, and confirms an internal release. Direct production publishing is refused.
- Preparation rejects an Android release when only an iOS native target is synced.
- The read-only remote doctor: detects an empty deployed channel, verifies a signed active manifest, artifact size, byte-range support and full SHA-256, and rejects corrupted delivered bytes.
- Guided setup preview and application in a synthetic Capacitor 8 host: app and platform detection, no changes in preview, create-only identity/provider output, public SQL and Edge trust values, private file permissions, and refusal to overwrite existing work.
- Cloudflare provider export from the actual package, Worker type checking and bundle dry run, plus local Workerd/D1/R2 integration: signed publication, rejected altered upload, create-only artifact write, byte ranges, rollback, withdrawal, replay, and competing channel promotions. A 200-request concurrent metadata sample completed successfully against the local Worker.
- Firebase provider export, Function type checking, and local Firestore/Storage emulator conformance: signed publication, replay and tamper rejection, create-only upload, byte ranges, competing promotions, rollback, withdrawal, and aggregate events. No live Firebase deployment was performed for 0.5.0.
- Provider SDK and scaffold packaged in the npm tarball; the exported scaffold typechecks. Deployment preflight tests verify explicit Cloudflare account and Firebase project/bucket scoping without touching remote resources.
- JSON Capacitor config fingerprinting: generated updater settings do not create a runtime loop; other native configuration changes still change the fingerprint. Symlinked native patch targets are rejected.

Run `npm run check` for the portable suite. Native Swift checks require macOS/Xcode; the Java corpus requires a JDK. Firebase emulator integration uses the command in its provider guide. SQL tests are opt-in with `DIRECT_OTA_SQL_TEST=1` and a **disposable local** PostgreSQL instance; see the provider guide. The GitHub workflow runs the SQL harness against a fresh PostgreSQL service.

## Fresh native fixture

A newly created Capacitor 8 project installed the 0.2.0 package tarball, ran guided setup, merged the generated settings, and synced the native projects. It compiled successfully for the iOS Simulator with Xcode and for Android with Gradle/OpenJDK 21. The final native overlays were used. After iOS package resolution, the runtime was regenerated, both platforms were rebuilt, and `direct-ota doctor` verified generated and copied native plugin settings. This native build evidence is from 0.2.0; versions 0.3.0 through 0.6.0 change publishing, provider, and setup tooling, not the native overlay.

## Isolated live Cloudflare test

On September 25, 2026, a dedicated test Worker, D1 database, and private R2 Standard bucket were created in the connected Cloudflare account. No production application resource was modified. The D1 migration applied remotely; the Worker deployed with generated synthetic public trust and a separate random upload secret. A synthetic iOS runtime published one encrypted 176-byte archive through the normal CLI. Live `doctor --remote` verified the signed manifest, artifact size, `206` byte range, and full SHA-256. A production test channel advanced through rollout, withdrawal, and rollback. Two simultaneous signed promotions produced one success and one conflict; replay and altered command signatures were rejected. A burst of 100 concurrent metadata checks succeeded, with observed p50 around 940 ms and p95 around 1,837 ms from this Mac and network. These are sample measurements, not a throughput guarantee.

After moving upload capabilities from URL queries into request headers, a second isolated Worker/D1/R2 deployment repeated the signed CLI upload and promotion. The live Worker rejected missing-header and query-token uploads with `403` and altered archive bytes with `400`. Remote doctor again verified the signed manifest, size, byte range, and SHA-256. The local integration test also covers a modified capability header. The second Worker, database, object, bucket, and synthetic local identity were removed; subsequent Wrangler listings showed no D1 databases or R2 buckets in this test account.

The account's R2 activation checkout showed a $0 base fee and usage-based overage terms. The temporary Worker, D1 database, R2 object, and bucket were deleted after the test. The R2 subscription was canceled and the dashboard showed it ending October 25, 2026; billable usage at that check was $0.00. Account-wide billing and quota history are outside this code test; operators must review them for their own rollout.

A successful compile proves native integration can build. It does not prove every OS/device/network combination, a healthy installed app, or an end-to-end physical-device update.

## Publication checks

The release is packaged from this separate repository, with no private application's Git history. The published source/history and package contents are checked for credentials and private application identifiers. The tarball is installed into a separate consumer directory to verify its CLI and package exports.

## Limits

- No independent security certification or 100,000-device load test is claimed.
- Firebase emulator conformance does not establish live Hosting/Functions/Firestore/Storage configuration, real-device activation, billing, or capacity. The 0.5.0 deployment command paths were locally preflight-tested with injected runners, not applied to a paid project.
- The live Cloudflare sample used one tiny artifact and 100 concurrent checks. It does not establish capacity for hundreds of thousands of phones, physical-device behavior, regional latency, or a zero-cost production fleet.
- Local SQL tests emulate the relevant Storage tables and roles. The separate Docker test exercises actual local Supabase Storage. Neither establishes hosted Edge Function, gateway, advisor, or capacity behavior.
- This generalized package has no recorded physical-device fleet acceptance. Adopters must verify their own first native release and both production channels.
- The standalone Node provider is a single-instance deployment with persistent local state. It is not a distributed database or a hosted service with an SLA.
- New runtime/backend adapters need their own tests and integration evidence before being advertised as supported.

Before broad production rollout, exercise interrupted downloads, network changes and cellular consent, force-quit recovery, storage failures, startup rollback, and the app's protected business actions on the actual supported platforms. Use staged deployment and record the observed results.
